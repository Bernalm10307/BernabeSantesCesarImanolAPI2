doctype html
html(lang="es")
  head
    meta(charset="UTF-8")
    title Respuesta
    style.
      body {
        background: linear-gradient(135deg, #ff9966, #ff5e62);
        font-family: Arial, sans-serif;
        color: #fff;
        display: flex;
        flex-direction: column;
        align-items: center;
        justify-content: center;
        height: 100vh;
        margin: 0;
      }
      h1 { font-size: 2rem; margin-bottom: 1rem; }
      p { font-size: 1.2rem; }

  body
    h1 Respuesta recibida ✅
    p Nombre: #{nombre}
    p Mensaje: #{mensaje}
